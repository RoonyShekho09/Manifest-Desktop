CREATE TABLE VehicleRecord (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL
);

insertVehicleType:
INSERT OR REPLACE INTO VehicleRecord(id, name)
VALUES (?, ?);

queryVehicleTypes:
SELECT * FROM VehicleRecord;

hasVehicleTypes:
SELECT EXISTS(SELECT 1 FROM VehicleRecord LIMIT 1);