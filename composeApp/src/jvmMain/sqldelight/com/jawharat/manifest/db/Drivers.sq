CREATE TABLE Driver (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    phone TEXT NOT NULL,
    destination TEXT NOT NULL
);

insertDriver:
INSERT OR REPLACE INTO Driver(id, name, phone, destination)
VALUES (?, ?, ?, ?);

queryDrivers:
SELECT * FROM Driver;

hasDrivers:
SELECT EXISTS(SELECT 1 FROM Driver LIMIT 1);