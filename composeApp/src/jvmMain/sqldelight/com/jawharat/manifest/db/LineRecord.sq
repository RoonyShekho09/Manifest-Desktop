CREATE TABLE LineRecord (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL
);

insertLine:
INSERT OR REPLACE INTO LineRecord(id, name)
VALUES (?, ?);

queryLines:
SELECT * FROM LineRecord;

hasLines:
SELECT EXISTS(SELECT 1 FROM LineRecord LIMIT 1);